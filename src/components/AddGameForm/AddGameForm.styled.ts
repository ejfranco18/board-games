import styled from 'styled-components';

export const AddGameFormStyled = styled.div`
  .form-wrapper {
    background: rgba(255,255,255,0.2);
    width: 50%;
    padding: 15px;
    border-radius: 20px;
    margin: 20px auto;
  }

  .game-title {
    font-style: italic;
  }

  .image-wrapper {
    margin: 0 auto 60px;
    width: fit-content;
  }
`;
